# FFXIV Kill Count Spawn Counter Front End

A React app to help users keep track of killing mobs to spawn S rank hunts in FFXIV.

## Installation 

To install run:
```bash
$ npm install
```

To run locally:
```bash
$ npm run start
```

To create a production build:

```bash
$ npm run build
```